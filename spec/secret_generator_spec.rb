require 'spec_helper'

describe SecretGenerator do
  it 'has a version number' do
    expect(SecretGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
